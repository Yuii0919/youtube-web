# 生產環境 / 前後端分離時：請填 Render backend 完整網址（不含結尾斜線）
# 例如：https://your-backend-name.onrender.com
VITE_API_BASE_URL=

# 本機開發可覆蓋 dev 代理目標；若留空，會優先使用 VITE_API_BASE_URL，再 fallback 到 http://localhost:8000
VITE_DEV_PROXY_TARGET=
